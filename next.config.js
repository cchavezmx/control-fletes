/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['s.gravatar.com', 'https://storage.googleapis.com'],
  },
}

module.exports = nextConfig
